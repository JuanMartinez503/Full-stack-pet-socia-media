<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>P🐾ST</title>
<link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/custom.css">
</head>
<body class="bg-gray-100">
  <header class="bg-white border-b border-gray-300">
    <nav class="container mx-auto px-4 py-2 flex justify-between items-center">
      <a href="/" class="text-lg font-semibold">Home</a>
            <a href="/post" class="text-lg font-semibold">Create Post</a>

      <div class="flex items-center space-x-4">
        {{#if logged_in}}
        <button class="bg-blue-500 text-white px-4 py-2 rounded" id="logout">Logout</button>
        {{else}}
        <a href="/login" class="bg-blue-500 text-white px-4 py-2 rounded">Login</a>
        {{/if}}
      </div>
    </nav>
  </header>

  <main class="container mx-auto px-4 py-8">
    <!-- Your main content goes here -->
    {{{body}}}
  </main>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>
